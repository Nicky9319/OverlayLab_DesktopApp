// Centralized environment configuration for the renderer process
// This is the single source of truth for environment selection.
// Valid values: 'development' | 'production'
// NOTE: For packaging, ensure this is set to 'production'. A prebuild check will enforce it.
export const ENV = 'production';


